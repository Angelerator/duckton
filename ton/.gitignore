# Acton main directory
.acton/

# Build directory (generated by the contract toolchain). Keep the compiled
# JobEscrow code cell tracked — the `ton-live` settlement build embeds it at
# compile time (include_str!), so it must exist in a clean checkout / CI.
build/*
!build/JobEscrow.json
dist/

.DS_Store
node_modules/

# VS Code
.vscode/*
.history/
*.vsix

# IDEA files
.idea

# Vim
Session.vim
.vim/

# Other private editor folders
.nvim/
.emacs/
.helix/

# Acton related
.env
.env.*
!.env.*.example
*.mnemonic
wallets.toml
global.wallets.toml
global.libraries.toml

# coverage
lcov.info
gen/
